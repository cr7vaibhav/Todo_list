{
  "name": "nodejs_sqlite",
  "version": "1.0.0",
  "description": "full stack todo app that used a NodeJS backend , a SQLite db and JWT auth",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --env-file=.env --experimental-sqlite ./src/server.js ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
